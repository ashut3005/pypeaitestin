name: CI Pipeline

on:
  pull_request:
    branches:
      - main
      - release
  # push:
  #   branches:
  #     - main

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show Trigger Information
        run: |
          echo "Pipeline Triggered!"
          echo "Source Branch (PR From): ${{ github.head_ref }}"
          echo "Target Branch (PR To): ${{ github.base_ref }}"
          echo "Commit SHA: ${{ github.sha }}"
          echo "Repository: ${{ github.repository }}"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test
        continue-on-error: false

      - name: Build project
        run: npm run build

      - name: List build output (debug)
        run: ls -R

  sonarqube:
    name: SonarQube Scan and Quality Gate
    runs-on: ubuntu-latest
    needs: test-and-build

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          projectBaseDir: frontend

      - name: Check SonarQube Quality Gate
        uses: SonarSource/sonarqube-quality-gate-action@v1
        timeout-minutes: 10
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}


# done



















# name: CI Pipeline

# on:
#   pull_request:
#     branches:
#       - main
#       - release
#   # push:
#   #   branches:
#   #     - main

# jobs:
#   test-and-build:
#     runs-on: ubuntu-latest

#     defaults:
#       run:
#         working-directory: frontend

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Show Trigger Information
#         run: |
#           echo "Pipeline Triggered!"
#           echo "Source Branch (PR From): ${{ github.head_ref }}"
#           echo "Target Branch (PR To): ${{ github.base_ref }}"
#           echo "Commit SHA: ${{ github.sha }}"
#           echo "Repository: ${{ github.repository }}"

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           # cache: npm
#           # cache-dependency-path: frontend/package-lock.json

#       - name: Install dependencies
#         run: npm install

#       - name: Run tests
#         run: npm test
#         continue-on-error: false

#       - name: Build project
#         run: npm run build

#       - name: List build output (debug)
#         run: ls -R



# # name: CI Pipeline

# # on:
# #   push:
# #     branches:
# #       - main

# # permissions:
# #   contents: write

# # jobs:
# #   test-and-build:
# #     runs-on: ubuntu-latest

# #     defaults:
# #       run:
# #         working-directory: frontend

# #     steps:
# #       - name: Checkout code
# #         uses: actions/checkout@v4

# #       - name: Set up Node.js
# #         uses: actions/setup-node@v4
# #         with:
# #           node-version: 20

# #       - name: Install dependencies
# #         run: npm install

# #       - name: Run tests
# #         run: npm test

# #       - name: Build project
# #         run: npm run build

# #       - name: List build output (debug)
# #         run: ls -R
